# TEMPLATE — substitute {{NOMBRE}} and, if you want, {{OBJETIVO}}.
[project]
name = "{{NOMBRE}}"
version = "0.1.0"
description = "{{OBJETIVO}}"
requires-python = ">=3.10"
dependencies = [
    # Versión verificada del SDK: 0.2.148 (empaca Claude Code 2.1.251).
    # El paquete trae el binario `claude` adentro: no hay que instalar nada más.
    "claude-agent-sdk>=0.2.148",
]

[tool.uv]
package = false
